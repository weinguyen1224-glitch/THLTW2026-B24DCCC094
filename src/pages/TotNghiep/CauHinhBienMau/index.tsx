import TableBase from "@/components/Table";import { type IColumn } from "@/components/Table/typing";import { useModel } from "umi";const CauHinhBienMauPage = () => {const { getModel, page, limit } = useModel("totnghiep.cauhinhbienmau");const columns: IColumn<any>[] = [];return <TableBase columns={columns} dependencies={[page, limit]} modelName="totnghiep.cauhinhbienmau" title="Cấu hình biên mẫu" />;};export default CauHinhBienMauPage;
