import TableBase from "@/components/Table";import { type IColumn } from "@/components/Table/typing";import { useModel } from "umi";const ThongKePage = () => {const { getModel, page, limit } = useModel("totnghiep.thongke");const columns: IColumn<any>[] = [];return <TableBase columns={columns} dependencies={[page, limit]} modelName="totnghiep.thongke" title="Thống kê" />;};export default ThongKePage;
