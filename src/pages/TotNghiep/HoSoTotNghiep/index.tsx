import TableBase from "@/components/Table";import { type IColumn } from "@/components/Table/typing";import { useModel } from "umi";const HoSoTotNghiepPage = () => {const { getModel, page, limit } = useModel("totnghiep.hosototnghiep");const columns: IColumn<any>[] = [];return <TableBase columns={columns} dependencies={[page, limit]} modelName="totnghiep.hosototnghiep" title="Hồ sơ tốt nghiệp" />;};export default HoSoTotNghiepPage;
