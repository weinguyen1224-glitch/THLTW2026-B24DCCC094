import TableBase from "@/components/Table";import { type IColumn } from "@/components/Table/typing";import { useModel } from "umi";const VanBangPage = () => {const { getModel, page, limit } = useModel("totnghiep.vanbang");const columns: IColumn<any>[] = [];return <TableBase columns={columns} dependencies={[page, limit]} modelName="totnghiep.vanbang" title="Văn bằng" />;};export default VanBangPage;
